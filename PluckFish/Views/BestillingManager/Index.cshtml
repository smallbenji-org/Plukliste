@model PluckFish.ViewModels.BestillingManagerViewModel
@{
    ViewData["Title"] = "Bestilling";
}

<div class="card">

    <div class="table-container">
        <div class="table-wrapper">
            <h3>Varer</h3>
            <div class="form-group search-container wide" style="margin-top: 1rem; margin-left: 0.5vw;">
                <input type="text" id="productSearch" class="form-control" placeholder="Søg Varer...">
            </div>
            <table class="pluk-table margin">
                <thead>
                    <tr>
                        <th>Vare</th>
                        <th>Handling</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var vare in Model.Varer)
                    {
                        <tr>
                            <td>@vare.Product.Name</td>
                            <td>
                                <button class="action-btn btn-add">Tilføj</button>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
        <div class="table-wrapper">
            <h3>Valgte Varer</h3>
            <table class="pluk-table">
                <thead>
                    <tr>
                        <th>Vare</th>
                        <th>Mængde</th>
                    </tr>
                </thead>
                <tbody>
                    @* @foreach (var item in Model.Items)
                    {
                        <tr>
                            <td>@item.Product.Name</td>
                            <td>
                                <form class="quantity-form">
                                    <input type="number" class="form-control quantity-input" value="0" />
                                </form>
                            </td>
                        </tr>
                    } *@
                </tbody>
            </table>
            <div class="checkout-container">
                <div class="checkout-list">
                    <p class="checkout-placeholder">Ingen varer valgt...</p>
                    @* eksempel: *@
                    <div class="checkout-item">
                        <span>Mock Produkt 1</span>
                        <span class="antal">x1</span>
                    </div>
                    <div class="checkout-item">
                        <span>Mock Produkt 2</span>
                        <span class="antal">x2</span>
                    </div>
                    <div class="checkout-item">
                        <span>Mock Produkt 3</span>
                        <span class="antal">x7</span>
                    </div>
                    <div class="checkout-item">
                        <span>Mock Produkt 4</span>
                        <span class="antal">x16</span>
                    </div>
                </div>
                <div class="checkout-footer">
                    <button class="action-btn btn-checkout">Bestil</button>
                </div>
            </div>
        </div>
    </div>
</div>